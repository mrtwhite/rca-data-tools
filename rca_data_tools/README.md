# Repository contents

``` bash
.
├── qaqc: The directory `qaqc` module that contains everything in regards to quality control.
│   ├── docker: Directory containing the Dockerfile to base the prefect image from.
│   └── params: Directory for a bunch of OOI related parameters csvs.
└── tests: Directory for software tests.
```
